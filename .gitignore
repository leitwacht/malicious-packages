# operator-reference only: packages OSV already covers (not for submission)
_already-in-osv/
# operator-reference only: candidates we couldn't confirm novelty for this run
_pending/
# scratch
*.tmp

# This is a DATA-ONLY public repo: only osv/ + index.json + README + LICENSE are
# tracked. The generation/deploy tooling is maintained privately and must never
# reach the public feed; it lives here on disk only because the feed is generated
# in place.
/scripts/
/deploy/
# operator bookkeeping (full catch roster incl. OSV-covered ones) — local only.
/manifest.json

# osv-scanner offline DB is published as a rolling GitHub Release asset
# (built by .github/workflows/build-offline-db.yml), never committed.
/osv-scanner/
